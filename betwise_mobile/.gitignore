.expo/

# Generated by `expo prebuild` — EAS Build regenerates these fresh from
# app.json/app.config.* on every build, so they shouldn't be committed
# (keeps the managed workflow's config-as-source-of-truth model intact).
/android/
/ios/
